# IntelliJ Platform Artifacts Repositories
# -> https://plugins.jetbrains.com/docs/intellij/intellij-artifacts.html
pluginGroup=com.fapiko.jetbrains.plugins.better_direnv
pluginName=better_direnv
# SemVer format -> https://semver.org
pluginVersion=1.4.2
# See https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html
# for insight into build numbers and IntelliJ Platform versions.
# Issue filed in https://youtrack.jetbrains.com/issue/PY-56172/RunConfigurationpatchCommandLine-Not-Called-In-20222
# causes an issue requiring an experimental API to fix, requiring a newer minimum version of the platform.
pluginSinceBuild=261
pluginUntilBuild=
# IntelliJ Platform Properties -> https://plugins.jetbrains.com/docs/intellij/tools-intellij-platform-gradle-plugin.html
platformType=IU
platformVersion=2026.1
# Java language level used to compile sources and to generate the files for - Java 21 is required for IntelliJ Platform Gradle Plugin 2.x
javaVersion=21
# Gradle Releases -> https://github.com/gradle/gradle/releases
gradleVersion=9.4.1
intellijPlatformVersion=2.18.1
# Gradle JVM memory settings - increased for IntelliJ Platform Gradle Plugin 2.x
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=1024m
# Configuration cache. `build`, `test` and `buildPlugin verifyPlugin` (the CI task
# set) all store and reuse an entry with 0 problems.
# Caveat: enabling this also makes Gradle run tasks of the *same* project in parallel
# (`--no-parallel` does not turn that off), which exposed a thread-safety bug in
# InstrumentCodeTask -- it drives the stateful, non-thread-safe `project.ant`, so a
# project's instrumentCode and instrumentTestCode could corrupt Ant's shared wrapper
# stack and fail intermittently with "1 >= 1". This is NOT limited to --rerun-tasks;
# it hit CI directly. The `mustRunAfter` workaround in the `allprojects` block of
# build.gradle.kts orders those two tasks and removes the race until the upstream fix
# (JetBrains/intellij-platform-gradle-plugin#2193) ships in a plugin release.
# Details and A/B repro in issue #72.
org.gradle.configuration-cache=true
